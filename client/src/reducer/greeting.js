import { createReducer } from 'src/util';

export const transformText = (str) => {
  const result = str.split('').map()
};

const initState = {
  text: []
};

const greeting = createReducer(initState);

export default greeting;
